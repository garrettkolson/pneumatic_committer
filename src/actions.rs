use std::sync::Arc;

pub struct ActionRouter {

}

impl ActionRouter {
    pub fn init() -> Self {
        ActionRouter {}
    }

    pub fn listen_for_registrations(router: Arc<ActionRouter>) {

    }
}